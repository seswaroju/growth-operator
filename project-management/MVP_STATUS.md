# MVP Ticket Status Ledger

One scannable row per ticket, **with the date it was implemented**. This is the quick
index; the sources of truth stay:

- **[IMPLEMENTATION_LOG.md](IMPLEMENTATION_LOG.md)** — full dated detail per completed ticket (append-only).
- **[CURRENT_TASK.md](CURRENT_TASK.md)** — the one active ticket.
- **[IMPLEMENTATION_AUDIT.md](IMPLEMENTATION_AUDIT.md)** — a one-time **2026-07-10 snapshot** (module-based, not ticket-based; do not treat as current).

_Last updated: 2026-08-07._

Legend: ✅ done · 🟡 partial · ⬜ not started

| Ticket | Title | Status | Implemented | Commit | One-line summary |
|---|---|---|---|---|---|
| MVP-001 | Monorepo scaffold | ✅ | 2026-07-10 | `cf7536e` | `core/` module tree, `web/` scaffold, migrations framework, docker-compose, CI, Makefile |
| MVP-002 | Dev docker-compose stack | ✅ | 2026-07-10 (data services verified live 2026-07-22) | `cf7536e` | Postgres+Redis compose; env-prefix bug fixed & services verified healthy during MVP-011; **app containers not yet booted** |
| MVP-003 | CI pipeline | 🟡 | 2026-07-10 | `cf7536e` | `.github/workflows/ci.yml` exists; a **green run on GitHub not yet confirmed** |
| MVP-004 | Migration framework + RLS helper | ✅ | 2026-07-10 (exercised live 2026-07-22) | `cf7536e` | Alembic async `env.py` + `migrations/lib/rls.py`; proven for real by migration 001 up/down |
| MVP-005 | Config loader + error taxonomy | ✅ | 2026-07-10 | `cf7536e` | Layered `config.py`; `errors.py` RFC7807 + 12 canonical codes |
| MVP-006 | OTel tracing + structured logging | 🟡 | 2026-07-22 | `684a000` | SDK wiring (env-gated) + JSON logs + PII scrubber (tested). End-to-end trace acceptance needs webhook→consumer→send (MVP-032+) |
| MVP-007 | Health + readiness endpoints | ✅ | 2026-07-22 | `684a000` | `/healthz` (liveness) + `/readyz` (pg+redis+migration-head); compose healthcheck; tested live |
| MVP-008 | Secrets via SOPS | 🟡 | 2026-07-22 | `684a000` | Scaffold: `.sops.yaml`, gitleaks pre-commit, decrypt script, boot fail-closed (tested). Age key + real `*.enc.yaml` are founder's step |
| MVP-009 | Staging environment | 🟡 | 2026-07-22 | `684a000` | **BLOCKED** — Terraform (Hetzner CPX21) + deploy workflow written but un-applied; needs account/domain/residency (BLOCKERS #8)/Meta |
| MVP-010 | Lint guards (core↛verticals, noun/money/send) | ✅ | 2026-07-22 | `684a000` | `scripts/guards.py` + allowlist + CI wiring; 8 tests (each guard red on its violation) |
| MVP-011 | OTP auth endpoints | ✅ | 2026-07-22 | `6cd38f4` | Migration 001; OTP logic + `/v1/auth/otp(/verify)`; interim **email** channel; `EmailOtpDelivery` (gated); **verified live** |
| MVP-012 | Sessions + JWT issue/refresh | ✅ | 2026-07-29 | `35457ef` | Refresh rotation + reuse-revokes-family + rotation-race on `sessions` (001); `/v1/auth/refresh`; `jti` nonce fix; **77 pytest** live. Audit-on-reuse interim (log) until MVP-024 |
| MVP-013 | Logout + revocation | ✅ | 2026-07-29 | `35457ef` | `POST /v1/auth/logout` + `/logout-all` on `sessions` (001); revoked session can't refresh; **80 pytest** live |
| MVP-014 | Organizations + /me | ✅ | 2026-07-29 | `35457ef` | Migration 002 (orgs + user_orgs +RLS + `app.user_id` self-policy); `POST /v1/orgs`, `GET /v1/me`; refresh re-embeds org_id; `apply_rls` NULLIF-hardened; **86 pytest** live. ⚠️ RLS not enforced until `app_rw` role (BLOCKERS #11) |
| MVP-015 | RBAC roles + @requires | ✅ | 2026-07-29 | `35457ef` | Migration 003 (roles/permissions/role_permissions + user_roles+RLS, seeded); `permissions.py` constants + `requires()` dep; 403 problem+json names perm; **101 pytest** live |
| MVP-016 | Tenant middleware + app_rw | ✅ | 2026-07-29 | `290c476` | `app_rw` non-BYPASSRLS role + 2-URL split (app vs migrator); `get_db`/`org_scoped_session` SET LOCAL; session-SET guard; **RLS now ENFORCED** (BLOCKERS #11 resolved); **107 pytest** + live smoke |
| MVP-017 | Staff invite (seed only) | ✅ | 2026-07-29 | `290c476` | `invites` (global, expiring, appended after 005); owner-only invite + accept-as-staff; `invites_enabled` gate; **118 pytest** |
| MVP-018 | API keys (service auth) | ✅ | 2026-07-29 | `290c476` | Migration 004 (api_keys +RLS + `resolve_api_key` SECURITY DEFINER); `require_key_scope` sets org ctx + scope + last_used; founder-only issuance |
| MVP-019 | Messaging migration 005 | ✅ | 2026-07-29 | `290c476` | 6 org-scoped +RLS (channels/contacts/conversations/messages/templates/suppressions) + webhook_events global; isolation probed as app_rw |
| MVP-020 | Packs migration 008 + seed | ✅ | 2026-07-30 | `dbab65a` | Migration 008 (packs/pack_installations+RLS/catalog_schemas/agent_archetypes+seed/agent_bindings/agent_instances+RLS); 5 archetypes byte-for-byte vs tool-permissions.yaml (support omitted, DECISIONS); **140 pytest** |
| MVP-021 | Tenant settings service | ✅ | 2026-07-30 | `47846af` | Migration 009 (tenant_settings+RLS); `resolve()` 4-layer+provenance, tighten-only autonomy, `resolve_at`, audited writes; `/v1/settings(/effective)`; **151 pytest** |
| MVP-022 | Feature flag service | ✅ | 2026-07-30 | `47846af` | Migration 009 (feature_flags, flag_rules); in-process snapshot `eval` (no-I/O, atomic swap, sticky bucket), fail-closed kill-switch, `flag_debt.py`; `/v1/flags/eval` |
| MVP-023 | CRM migration 011 | ✅ | 2026-07-30 | `25527c0` | Migration 011 (leads/appointments/orders/attributions/segments +RLS); `last_customer_msg_at` trigger; money minor-units; **156 pytest** |
| MVP-058 | Prompt registry | ✅ | 2026-07-30 | `25527c0` | Migration 010 (prompt_layers partial-RLS+content-immutable, prompt_bindings+RLS one-active, prompt_evals); `registry.py` pin/compat/revert; HTTP endpoints deferred |
| MVP-026 | Consumer framework | ✅ | 2026-07-30 | `ee2ed43` | `@consumer` + XREADGROUP/XAUTOCLAIM drain, graceful shutdown; **168 pytest**. **Entrypoint wired 2026-08-04 (#16, `ac4ab52`)** — `core/worker.py` runs outbox publisher + all 3 consumers |
| MVP-027 | Consumer dedupe | ✅ | 2026-07-30 | `ee2ed43` | `dedupe_consumer` insert-first → exactly-once effect; 30d prune (`dedupe_prune` now scheduled daily, #16) |
| MVP-028 | Scheduler process | ✅ | 2026-07-30 | `ee2ed43` | cron matcher (no croniter) + tz-local firing + per-(job,minute) redis lock. **Entrypoint wired 2026-08-04 (#16, `ac4ab52`)** — `core/scheduler.py` installs+fires ladder/trust/embeddings/prune (live-broker smoke) |
| MVP-029 | Retries + DLQ | ✅ | 2026-07-30 | `ee2ed43` | bounded retries → `gop:dlq:<type>` + alert.ops; `dlq-replay.py` |
| MVP-030 | Typed event catalog | ✅ | 2026-07-30 | `ee2ed43` | `gen_events.py` → `types.py` (specs+checksum) drift test; `emit()` validates payloads |
| MVP-032 | Webhook ingress + sig verify | ✅ | 2026-07-30 | `33a76bc` | `/webhooks/whatsapp` constant-time HMAC verify + dedupe + quarantine, always-200; **174 pytest** |
| MVP-033 | Message normalizer | ✅ | 2026-07-30 | `33a76bc` | webhook→contact/conversation/message + `msg.received.v1` emit; `resolve_channel` fn; idempotent |
| MVP-031 | WhatsApp WABA connect | ✅ | 2026-07-30 | `2160890` | Migration `cfd462c65ec9` (`channel_credentials`+RLS); `POST /v1/channels/whatsapp/connect` token/handshake/echo gates (gated-simulated), Fernet-encrypted creds at rest, cross-org 409, health probe; **183 pytest** (merge `644b334`) |
| MVP-034 | Send adapter (gated) | ✅ | 2026-07-30 | `1cd8615` | `send.py`: single outbound exit, 4 fail-closed gates (audit capability / execution token / suppression / consent), 429 Retry-After + 5xx×3 retries, `msg.sent`/`msg.failed` + audit outcome; no migration; **191 pytest** (merge `59eb28b`) |
| MVP-048 | Embeddings + hybrid RRF | ✅ | 2026-07-31 | `308c578` | `embed.py` gated-simulated embedder + batch job; `search.py` RRF(k=60) fuse BM25⊕kNN(HNSW `<=>`) + filter pushdown + empty→nearest; `/catalog/search` returns `{results,nearest}`; real provider deferred (#16); **323 pytest** |
| MVP-047 | Text search (BM25) | ✅ | 2026-07-31 | `9c78ceb` | `search.py` search_text tsvector (simple⊕english) from title/desc/x-search projection, maintained on write; `ts_rank` BM25 over GIN; `GET /v1/catalog/search`; **318 pytest** |
| MVP-046 | Attributes validation (JSON Schema + CEL) | ✅ | 2026-07-31 | `63cb525` | `validate.py` Draft-2020-12 (+additionalProperties) + CEL constraints → `{path,error,rule}`, cached per (pack,ver); wired into CRUD → 422; **315 pytest** |
| MVP-059 | Composer + tenant layer generator | ✅ | 2026-08-01 | `7a2e0ff` | `composer.py` base+vertical+tenant render (immutable cache, strict params → MissingParam, sha256 hash reproducible, fail-closed); `tenant_layer.py` settings-baked layer (hash-versioned, idempotent); `prompts/base/concierge.md`; **336 pytest** |
| MVP-042 | Catalog schema registration + index gen | ✅ | 2026-08-01 | `0cee988` | Migration `1b9dc38df16c` (`generated_ddl`); `indexes.py` DDL gen from x-index (btree/numeric-cast/GIN) wired into installer + `apply_generated_indexes` CONCURRENTLY (lock_timeout 3s, IF NOT EXISTS); jewelry→6 indexes; **328 pytest** |
| MVP-050 | Pricing migration + rules_v1 engine | ✅ | 2026-08-02 | `48d5052` | Migration `63bcec3ea528` (013 pricing tables +RLS); safe-AST Decimal engine (round/rate/tax_rule/sum/min/max/map/item/offer, residue fail-closed, provenance, stale_rate) + registry; **both packs' goldens pass on one engine**; **351 pytest** |
| MVP-052 | Quote service/API + replay | ✅ | 2026-08-02 | `d57eab5` | `service.py` `compute_quote` (pre-load fresh snapshots → sync rate lookup; write quote+ledger **atomically**) + `replay_quote` (**byte-exact** from pinned `rate_snapshot_ids`); `api.py` `POST /v1/pricing/compute`(409 stale_rate)/`replay`, `GET /v1/rates/status` (`requires(CATALOG_READ)`); no migration; **357 pytest** |
| MVP-053 | Committed-figures ledger | ✅ | 2026-08-02 | `d57eab5` | `ledger.py` `write` (total + every positive breakdown line, with expiry) + `match` **exact tolerance 0**, unexpired, 48h window → off-by-one/expired fail closed (input to MVP-054 send gate); `figures_from_breakdown`; covered by `test_pricing_service` |
| MVP-054 | Send-path figure check | ✅ | 2026-08-02 | `fea254a` | `extract.py` pure-Python Indian money parser (₹/Rs/INR, lakh/crore/k, Indian grouping, paise, Decimal, conservative — phone/id/time ignored); `send.py` **Gate 5** ledger.match → **422 `unledgered_figure`** before any Meta call, `figure_check` block/warn/off + audited tier-3 override; no migration; **378 pytest** (mt-* corpus + allow/block/partial/warn/override) |
| MVP-076 | Imports migration + batch API | ✅ | 2026-08-05 | `9fcee88` | Migration `3c7f4aa8f204` (`import_batches`+`import_rows`, +RLS). `state.py` batch state machine (legal-only `advance`; `failed` resumable). `service.py` create (caps ≤500MB/≤200img/≤5k CSV rows→422+chunking hint) + `transition` + list; emits `import.batch_state`. `api.py` (registered): `POST /v1/imports` multipart + list/get/rows + **SSE** relay (`StreamingResponse` off the Redis event stream). Dep: **python-multipart** (founder-approved). **576 pytest** (state property, caps+hint, SSE<2s, multipart create, service transitions, isolation). Extraction/review/load = 077–080; **Excel** via openpyxl at 078 |
| send-loop | Close the send loop (approved reply goes out) | ✅ | 2026-08-05 | `91faa96` | `_messages_send` (was a stub) now runs the gated `send()` (mint audit cap + execution token in the proxy session; simulated Meta; records the outbound message; `SendRefused`→structured). `send()` +optional `session` (reuse the caller's txn — a 2nd `org_scoped_session` deadlocks on the per-org advisory lock). Executor routes the reply through `messages.send` at RESPOND: plain reply auto-sends (tier1), priced reply parks (`_park_send`) → sends on approve; reject sends only the safe close. **559 pytest** (send/refuse/route/park/full-real-proxy-run/approve→send). **Completes objective steps 5–9.** Real Meta + quote figure_refs deferred |
| exec→composer | Prompt activation pipeline (grounded drafts) | ✅ | 2026-08-05 | `af93f06` | `base_layers.ensure_base_layer` (platform base seed from `prompts/base/`), installer `_activate_prompts` step (per concierge instance+task: gen tenant layer + `pin_binding` base+vertical+tenant; binding task→vertical anchor), executor `Deps.compose`/`_make_compose` → `composer.render` with **skeleton fallback**; `composed_prompt_hash` now grounded. Base concierge bumped 1.0→1.4. **553 pytest** + smoke (install→4 bindings; run composes real base+vertical+tenant). Concierge only (other archetypes lack base layers→skeleton). **Completes grounded drafts** |
| #20 | Tool→action bridge (pack tiers fire) | ✅ | 2026-08-05 | `889bb7e` | `engine.evaluate_tool`/`resolve_actions` map a tool call to its abstract-action **family** (max tier, pooled contributors, fallback once); `messages.send` is *also* `action.quote.send` when it carries a price (structured or figure-in-body via MVP-054). Proxy `_engine_tier`→`evaluate_tool`. Jewelry pack optional-attr conditions `has()`-guarded. **549 pytest** + real-proxy smoke: plain reply→tier1 (auto), ₹1.5L quote→tier2 (parks). Resolves BLOCKERS #20; completes MVP-044's tiering half of grounded drafts |
| MVP-044 | Pack seeding: policies/prompt layers | ✅ | 2026-08-05 | `c27872c` | `_seed_policies` seeds `approval_policies` (scope=pack) from bindings `tier_defaults` (`action_type`=`applies_to` verbatim; 30m→1800s, hold_and_remind→hold, approver→chain); idempotent; removed from `DEFERRED_STEPS`. Prompt-layer seeding was **already** implemented (9 candidate layers on install). Migration `b6456b200baa` — tight pack-only INSERT RLS (`p_pack_ins`; app_rw seeds `scope=pack` but not `core`). **538 pytest** (seed-vs-pack diff=∅, idempotent, field mapping, RLS-tightness) + live smoke. **Flagged:** tool→action bridge (BLOCKERS #20) — seeded tiers don't fire until the proxy maps tool→abstract-action; drafts fail-safe tier-2 meanwhile. `workflow_definitions` seeding deferred to MVP-072 |
| MVP-056 | Planner routing | ✅ | 2026-08-04 | `e1b274c` | `planner.py` consumer on `msg.received` → **classify** (gated-simulated longest-keyword match over pack `intent_keywords`) → **route** (intent→archetype/task via `taxonomy.py` loaded from the pack; ticket said "from agent_bindings" but intents aren't persisted there—flagged, DECISIONS) → **3 guards** (tenant paused=org.status, suppression, frequency cap; inbound=transactional=exempt) → **start_run** to the org's active instance; unclassifiable→concierge+clarify. Pack `intent_keywords` added (16 intents). No migration/dep. **534 pytest** (routing_golden 20/20, guard matrix, consumer path) + worker registers the consumer |
| MVP-064 | Model routes + failover | ✅ | 2026-08-04 | `232f8e5` | Migration `3680972ace7a` (`costs_lite` +RLS + `model_routes` seed: default/classify/converse/campaign, anthropic→openai; ahead of doc order—flagged). `model.py` +`SimulatedProvider`/`get_provider` **gated** registry (every provider→simulated until `llm_provider_enabled`). `routing.py` `RoutingModel` — route lookup (→default→fail-safe), **primary→fallback failover**, per-attempt cost→`costs_lite` (route+run), **all-down→holding template + alert**, zero successful LLM. Executor builds RoutingModel where `default_model()` was used. **518 pytest** + live executor→routing smoke. Real vendors + real pricing deferred to go-live (Option A) |
| MVP-063 | Failure contract + circuit breaker | ✅ | 2026-08-04 | `288427e` | Migration `da3474bd3cdb` (`incidents` +RLS; lands ahead of 018/MVP-074—flagged, DECISIONS; `circuit_open` already an allowed instance status). `failure.py` — `note_failure` (tier-2→incident w/ run link + `trust.record_incident`; 2nd consecutive→**circuit_open** + `alert.ops` + incident), `note_success` reset, `is_circuit_open`, `close_circuit` (reactivate + resolve). Executor: `start_run` **holds** on open circuit; `_drive` retries a hard `provider_unavailable` step **once** then trips; proxy converts a raising tool → `provider_unavailable`. **506 pytest** (+isolation). <30s alert-delivery + retry/threshold config deferred |
| MVP-062 | Budgets, rate windows, untrusted narrowing | ✅ | 2026-08-03 | `3752c3f` | `limits.py` **sliding** 60s rate window (sorted set, per instance+tool), daily budget check/record counters, **untrusted-narrowing lifecycle** (mark on external content → narrow to allow-list → clear at human boundary); wired into proxy (steps 3/5/6) + executor resume; no migration; **500 pytest** (incl. web_fetch→messages.send denied AC). Budget-record-at-send + telemetry table deferred |
| MVP-061 | Manifest compiler + signing | ✅ | 2026-08-03 | `870d565` | `manifest.py` compile L1 archetype ∩ L2 pack ∩ L3 tenant + **ed25519 sign/verify** + `recompile_instance` (re-pins instance); proxy now verifies **sig + hash + freshness** per call (060 was hash-only), executor pins the body hash; config `manifest_signing_seed`; no migration; **493 pytest** (intersection, stale→denied-until-recompile, tamper→sig-fail→abort). Tenant-grants source + auto-recompile trigger deferred |
| MVP-070 | Trust ledger job | ✅ | 2026-08-03 | `ef0ce5e` | Migration `30b7edf76a9d` (approvals.trust_settled marker; ticket said "trust_ledger rows"—flagged, DECISIONS); `trust.py` hourly `settle` (+1/72h-clean tier-2, idempotent) + `record_incident` (reset + 14d self-expiring tightening) + `demotion_offers` (**digest-only, never auto-apply**, IDL-007); hourly job registered; **484 pytest** (incl. 72h boundary). Incident detector + scheduler firing deferred |
| MVP-068 | WhatsApp interactive approvals + ladder | ✅ | 2026-08-03 | `22023dd` | Migration `bb65660f0771` (approvals notification-state cols; ticket said "none"—flagged, DECISIONS); `notify.py` render card + interactive ✅/❌ buttons + consumer on `approval.requested` (gated-sim send); button + text ✅/❌ fallback → `service.resolve`; **ladder** remind 50%/escalate 75%/expire (per-org, registered); **478 pytest**. Real Meta send + scheduler firing + normalizer inbound deferred |
| MVP-069 | Approval-parked run resume | ✅ | 2026-08-03 | `4d63e69` | Executor `tool_call`→**proxy.call**; tier-2 `ApprovalPending`→**park** (create approval + checkpoint pre-tool_call + interrupt); `resume_after_approval` **idempotent** (approve→re-run tool once via `RunContext.approved`; reject→customer-safe close, no original send); `resume.py` consumer on `approval.resolved` (dedupe + run-status = exactly once); no migration; **454 pytest** |
| MVP-067 | Approval service + resolve API | ✅ | 2026-08-03 | `bd84d39` | Migration `9f90c8831001` (`approvals` object +RLS); `service.py` create (`approval.requested`) + `resolve` **FOR-UPDATE idempotent** + **edit re-eval** (raise-tier→reject w/ explanation) + **410 expired** (`approval.resolved`); `api.py` `GET /v1/approvals`, `POST /{id}/resolve`; notify→068, resume→069; **449 pytest** |
| MVP-066 | Execution tokens | ✅ | 2026-08-03 | `8bc4b5a` | `tokens.py` ed25519 mint/verify — signed `{jti,ctx_hash,tier,exp}`, **single-use jti** (replay rejected), ctx-bound (swap→bad sig), 10m expiry; config `execution_token_signing_seed`; **stub removed** (send.py Gate 2 → real verify; normalizer mints real token; zero stub refs in core); no migration/dep; **442 pytest**. Campaign-executor verify + jti prune deferred |
| MVP-065b | Audit hardening (CEL fail-safe + isolation tests) | ✅ | 2026-08-02 | `5902caf` | `_matches` fails safe on any CEL compile/eval error (rule still counts → can only tighten); + `tests/isolation/test_batch_rls.py` cross-tenant tests for quotes/committed_figures_ledger/approval_policies (probed as app_rw); **436 pytest** |
| MVP-065 | Policy engine | ✅ | 2026-08-02 | `528bcbe`⚠️ | Migration `1993ba538f4f` (014: approval_policies custom-RLS + trust_ledger/incident_tightening/execution_token_jti +RLS); `engine.evaluate` (core-tier4→pack→tenant→incident, CEL, **max-tier-wins**, matched ids); **deterministic (10k shuffles)** + CEL compile cache (**p95 0.88ms**); `validate_tenant_rule` **tighten-only**; **mediation tier check now live** (proxy→engine); token minting→066; **433 pytest** |
| MVP-060 | Mediation proxy | ✅ | 2026-08-02 | `1169694` | `proxy.call` ordered chain (manifest→narrowing→params→rate→budget→tier→audit→execute→egress); structured recoverable ToolError; **≥3 violations abort** run; out-of-manifest **denied+audited+alerted**; `tools.py` registry (catalog.search/pricing.compute/ledger.read wired, messages.send→tier-2, calendar/crm stubbed); new **runtime-not-tools** guard (no import-linter dep); tier stubbed→MVP-065; no migration/dep; **419 pytest** |
| MVP-055 | Executor skeleton + checkpoints | ✅ | 2026-08-02 | `4824fb6` | Migration `f124e1102952` (015: agent_runs/steps/memory +RLS, model_routes; runs carry composed_prompt_hash + permission_manifest_hash); **LangGraph** graph route→compose→model_turn→tool_call→respond + durable executor (Redis+PG checkpoint/node, kill/budget/timeout, idempotent respond); gated-sim LLM; `GET /v1/ops/runs/{id}`; **chaos-kill 10/10 no dup send**; langgraph dep approved (DECISIONS); **408 pytest** |
| MVP-051 | Rate ingestion + manual entry | ✅ | 2026-08-02 | `7d95a7a` | `rates.py` `ingest_rate` (bounds → quarantine, staleness clock untouched) + `fetch_and_store` (alert.ops on quarantine / rate.updated on success) + `record_manual_rate` (audited, keys only); gated `SimulatedRateFetcher` default, real IBJA fails closed (#5); `POST /v1/rates/manual` (ORG_MANAGE); staleness→409 via engine gate; no migration; **396 pytest**. Tier-2 approval + scheduler firing deferred |
| MVP-049 | Availability + price-input staleness | ✅ | 2026-08-02 | `33ac11e` | `availability.py` validated transition graph (+audit chain on agent-actor change) + `price_input_deps` rule-AST extractor (jewelry→{net_weight_g,purity,stones,requested_discount_minor}) → flags open quotes `stale_inputs` when a rule-referenced attr changes (via `inputs.item_id`), wired into `crud.update_item`; typed `catalog.price_inputs_changed` event deferred (BLOCKERS #17); no migration; **386 pytest** |
| MVP-045 | Catalog migration + CRUD | ✅ | 2026-07-31 | `170eec0` | Migration `d2cecc53f63c` (012: catalog_items+history+idempotency, RLS, GIN+HNSW, pgvector); `crud.py` create/get/list-cursor/update-ifmatch/soft-delete + history + identity dedup + Idempotency-Key; `POST/GET/PATCH/DELETE /v1/catalog/items`; **307 pytest** (unblocks 042) |
| MVP-043 | Kirana dry-run CI gate | ✅ | 2026-07-31 | `b70a4b1` | `installer.dry_run` (full pipeline in a rolled-back txn, validates+plans, no writes); `verticals/kirana/install.yaml` + `tests/e2e/test_kirana_dryrun.py`; CI gate proves 2nd pack installs with zero core changes; **291 pytest** |
| MVP-041 | Jewelry install e2e fixture | ✅ | 2026-07-31 | `527412a` | `verticals/jewelry/install.yaml` + `tests/e2e/test_jewelry_install.py` (assert expected_result field-by-field, <60s); wired into CI `migrate` job (app_rw + e2e) as a required check; **290 pytest** |
| MVP-040 | Transactional installer + API | ✅ | 2026-07-31 | `9fa3ac3` | `installer.py` 6-step atomic pipeline (2 deferred #14) + digest idempotency + rollback + uninstall; migration `5dcbda42efca` (+`failed` status); `GET/POST/DELETE /v1/packs(/installations)`; **289 pytest** |
| MVP-039 | Bundle parser + verifier | ✅ | 2026-07-31 | `db28412` | `core/packs/bundle.py`: prompt anchor split → layer records, `parse_pack_dir` (per-file contract validation, path+file-named errors), digest manifest + ed25519 verify (tampered refused), `load_bundle` dev/prod; no new deps (.tar.zst deferred #13); **279 pytest** |
| MVP-038 | Pack contract models | ✅ | 2026-07-31 | `9cda64f` | `core/packs/contracts.py` typed L0↔L1 contracts (manifest+slots, bindings, catalog, pricing, workflow, integration + aux); strict/open split; every verticals/* file parses + path-precise negatives; **266 pytest** |
| MVP-037 | Media handling | ✅ | 2026-07-31 | `b0a3bd0` | `media.py` inbound mime/size gates + fail-closed AV scan (quarantine+`alert.ops`) + object store → `messages.media`; outbound upload helper; **simulated** scanner/store (no new deps, real clamav/MinIO gated — BLOCKERS #12); normalizer wired; **225 pytest** |
| MVP-035 | Templates management | ✅ | 2026-07-31 | `1eb7f5f` | Migration `83efabba79ee` (message_templates Meta cols + `channels.waba_id` + `resolve_channel_by_waba`); `templates.py` CRUD/submit/status/gate/drainer; `send()` template path; `GET /v1/channels/whatsapp/templates`; jewelry_v2 seed YAML + gated script; **214 pytest** (merge `a06be80`). Real Meta submit/webhooks gated (#3) |
| MVP-036 | Suppression + consent | ✅ | 2026-07-30 | `9e3a11d` | Enforcement gate in MVP-034 + `keywords.py` STOP/UNSUB net (en/hi/te) → normalizer auto-suppress (marketing, idempotent) + founder-approved transactional confirm (DECISIONS 2026-07-30); **206 pytest** (merge `301edf3`). Suppressed badge deferred to chats page (MVP-087) |
| MVP-024 | Audit chain writer | ✅ | 2026-07-30 | `dbab65a` | Migration 006 (audit_log append-only +RLS +trigger, dedupe_consumer); per-org hash chain writer + 10m capability + `audit-verify.py`; p95 **1.12ms**; **131 pytest** |
| MVP-025 | Outbox emit + publisher | ✅ | 2026-07-30 | `dbab65a` | Migration 007 (event_outbox global); `emit` same-txn + `publish_batch` at-least-once → Redis streams (CloudEvents); crash-window + idempotency; **137 pytest** |
| Support-01 | Support tickets (GO control-plane #1) | ✅ | 2026-08-05 | `4140917` | Migration 018 (`support_tickets` +split-by-command RLS with fail-closed **platform-admin** exception; `platform_admins` allowlist); `core/support` owner raise/list + operator cross-tenant queue/resolve (audited); `core/tenancy/platform_admin.py` (`get_admin_db`); `web/` owner "Report an issue" + operator board; **599 pytest** (+23) |
| Support-01-sec | Operator-plane enterprise hardening | ✅ | 2026-08-06 | `4140917` | Least-privilege lock (guard test: `app.platform_admin` in exactly 1 table); migration 019 append-only **`platform_access_log`** (cross-tenant reads+writes, immutable); migration 020 `platform_admins.expires_at` (expired=fail-closed) + `revoke` script; `admin_plane_enabled` default-off (404s `/v1/admin/*`); gated fixed dev OTP; deploy-time controls in DECISIONS; **623 pytest** (+24) |
| Phase-1.1 | Tenant RBAC (owner/manager/staff/viewer) | ✅ | 2026-08-06 | `c338eb4` | Retired `founder` + `platform:admin` (closed a latent cross-tenant escalation); `permissions.py` 4 roles + full grid + rank/`can_grant_role`; migration 021 (CHECK widen + RBAC-catalog reseed, drift-tested); invites carry a role (rank-gated); re-gated `api_keys`+`ops`→`org:manage` |
| Phase-1.2 | Platform RBAC (dev/admin/staff/analyst) | ✅ | 2026-08-06 | `c338eb4` | `platform_permissions.py` separate namespace + `require_platform()`; migration 022 `platform_admins.role`; support endpoints role-gated; `grant-admin --role`; **plane-separation proven** (tenant⊥platform); **673 pytest** (+50) |
| Phase-2.1 | Operator identity `GET /v1/admin/me` | ✅ | 2026-08-06 | `804e889` | `platform_router.py` returns operator role+permissions; admin-plane gate moved to shared home; 403/401/404 gates; **680 pytest** (+7) |
| Phase-2.2 | Customer app (`web/`) | ✅ | 2026-08-06 | `804e889` | react-router + role-aware shell (`/v1/me`); auth context (localStorage); support screens + **invite-with-role** Team section; operator queue removed; `make make-owner`; **vitest 10/10**, tsc/build green |
| Phase-2.3 | Operator app (`web-ops/`) | ✅ | 2026-08-06 | `804e889` | New separate Vite app (port 5174); login gated on `/v1/admin/me`; role-aware nav by returned permissions; cross-tenant support queue + resolve; off-by-default; **vitest 6/6**, tsc/build green |
| Phase-3.1 | Customer dashboard: Home + shell | ✅ | 2026-08-07 | `9a6ebb5` | `core/insights` `GET /v1/dashboard/overview` (RLS + `insights:read`, 4 org-scoped KPI counts); `web/` full role-gated 8-section shell + Home KPI tiles (loading/empty/error) + section placeholders; **6 pytest + vitest 16** |
| Phase-3.2 | Approvals queue (HITL core) | ✅ | 2026-08-07 | `9a6ebb5` | `GET /v1/approvals` + `matched_rules` + typed model; `ApprovalsSection` queue — draft/"why"/price → approve / reject(+reason) / edit-then-approve (tier-raise guard preserved); viewer read-only; **7 pytest + vitest 6** |
| Phase-3.3 | Conversations & leads | ✅ | 2026-08-07 | `9a6ebb5` | new read module `core/conversations` — `GET /v1/conversations` (inbox + last-message), `/{id}` (thread), `/v1/leads` (pipeline), RLS + `conversations:read`; `ConversationsSection` inbox master-detail + pipeline board; **6 pytest + vitest 5** |
| Phase-3.4 | Catalog & pricing (frontend) | ✅ | 2026-08-07 | `9a6ebb5` | catalog backend already complete → `CatalogSection` searchable grid (static ₹ vs computed "Live rate"), create/edit/archive gated `catalog:write`, surfaces attribute-validation 422s; `lib/catalog`; **vitest 31**; existing catalog pytest green (no backend change) |
| Phase-3.5 | Customers / CRM | ✅ | 2026-08-07 | `9a6ebb5` | new read module `core/customers` — `GET /v1/customers` (+lead/order counts), `/{id}` (profile + leads + conversations + orders), RLS + `customers:read`; `CustomersSection` master-detail w/ purchase history; **5 pytest + vitest 34** |
| A1 | Analytics: event-facts + rollup | ✅ | 2026-08-07 | `ab1aed0` | migration 023 `business_metrics` (+RLS); `core/insights` compute/rollup (scheduled `business_metrics_rollup` daily, per-org, idempotent) + `GET /v1/insights/summary` (WoW) + Home "This week" card. **6 pytest + vitest 41** |
| A2.1 | Analytics: campaigns model + persistence | ✅ | 2026-08-07 | `ab1aed0` | migration 024 `campaigns` (+RLS); `core/campaigns` service/consumer/api (`campaign.executed` consumer records send counts, wired; `campaigns:send`/`:read`). **Honest:** campaign.* events not emitted yet (consumer ready). **7 pytest** |
| A2.2+A3.1 | Analytics: exact attribution + funnel + significance | ✅ | 2026-08-08 | `52f9776` | migration 025 `campaign_touches` (+RLS); deterministic **first-touch** attribution + funnel + one-sample z-test + drop-off (`analytics.py` pure); `GET /v1/campaigns/{id}/analytics`. Multi-touch deferred (backlog). **8 unit + 4 integ** |
| A3.2 | Analytics: ROI + layered drivers | ✅ | 2026-08-08 | `52f9776` | **unhackable ROI** (revenue only from immutable orders; cost = real sent×owner rate; org-isolated) + plain-language **drivers** (verdict→reasons); `campaign.cost_per_message_minor` setting. No migration. **4 unit** |
| A4.1 | Intelligence: agent-report framework | ✅ | 2026-08-08 | `52f9776` | migration 026 `agent_reports` (+RLS) — layered insight record (verdict→drivers→full_breakdown→evidence); `core/insights/reports` + `GET /v1/insights/reports(/{id})` (`insights:read`). **6 pytest** |
| A4.2 | Intelligence: campaign-analysis producer | ✅ | 2026-08-08 | `ffead49` | `core/campaigns/producer` — engine → stored `campaign_analysis` insight (deterministic, `model=deterministic`); `POST /v1/campaigns/{id}/report` + `verdict_line`. **3 pytest** |
| A4.3 | Intelligence: tracked competitors | ✅ | 2026-08-08 | `ffead49` | migration 027 `tracked_competitors` (+RLS); `core/competitors` CRUD (`campaigns:send` write / `insights:read` view). **4 pytest** |
| A4.4 | Intelligence: simulated competitor/marketing agents | ✅ | 2026-08-08 | `ffead49` | `core/insights/agents` — deterministic competitor + marketing producers, **gated-simulated** (`llm_provider_enabled` off→simulated; on→fail-closed `provider_unavailable`); `POST /v1/insights/reports/generate` (`campaigns:send`). **4 pytest** |
| A4.5 | Intelligence: owner⇄GO thread | ✅ | 2026-08-08 | `8e4e1e1` | migration 028 `insight_messages` (+RLS) — **split-RLS w/ scoped operator INSERT** (owner posts own; operator answers cross-tenant, `author_type='operator'` only); `resolve_report_org` SECDEF; owner GET/POST + operator `POST /v1/admin/insights/.../reply` (audited); **least-privilege lock updated** (2 allowed tables) + teeth-tested (owner can't forge operator msg). **4 pytest + isolation** |
| A4.6 | Intelligence: owner Insights UI | ✅ | 2026-08-08 | `9bbb031` | **frontend-only** (`web/`) — Insights section: report list → **leveled drill-down by intensity** (verdict→drivers→numbers→evidence, self-serve, no AI) + free-text **Ask Growth Operator** thread (human operator answers, no fabricated reply). New `api.ts` fetchers, `lib/insights` helpers + `QUESTION_LEVELS`, `InsightsSection.tsx`, `/insights` route + nav gated `insights:read`. **+14 vitest** (47 total); lint/tsc/build green; guards 0. |
| MVP-080 | Bulk import I3: load + 30-day revert | ✅ | 2026-08-09 | `79adc2f` | **The payoff — confirmed rows become catalog items.** `core/ingestion/load.py`: `load_batch` (advance `review→loading`; each confirmed row → `crud.create_item` [validates + pack identity dedup], stamped `import_batch_id`; `DuplicateIdentity`→skipped, `ValidationProblems`→load_failed, per-row; → `loaded`); `revert_batch` (**≤30 days**: archive this batch's UNMUTATED items, list edited-since ones as `mutated_skipped`; → `reverted`); `reap_old_batches` daily job (frees staging rows/blob for terminal batches >30d). `POST /v1/imports/{id}/load|revert` (`catalog:write`). `catalog_items.import_batch_id` already existed (mig 012). **+3 pytest** (load 2/2 stamped + revert archives; duplicate-sku skipped; mutated item left alone). |
| MVP-079 | Bulk import I2: review queue | ✅ | 2026-08-09 | `b4bc5c2` | `core/ingestion/review.py`: **validate** (advance `extracted→validating→review`; flag `missing_title` blocking + `duplicate_sku` [catalog + intra-batch]); **confirm / edit→re-flag→confirm / reject** a row; **bulk confirm-all** (skips rejected + blocking); **auto-approve gate** (all rows ≥0.95 conf AND no flags AND ≥5% sample confirmed). Endpoints `POST /v1/imports/{id}/validate`, `/rows/{seq}/confirm|reject`, `PATCH /rows/{seq}`, `POST /rows/confirm-all?auto=` (`catalog:write`). No migration/dep. **+3 pytest** (validate flags + lifecycle; bulk skips rejected/blocking; auto-approve needs conf+sample). |
| MVP-078 | Bulk import I1: CSV/XLSX extract + mapping | ✅ | 2026-08-08 | `ca763a0` | First bulk-import stage (closes part of "catalog is imported"). `core/ingestion/extract_csv.py`: parse **CSV** (stdlib) + **XLSX** (openpyxl) → one `import_rows` per row (`raw` + `normalized` mapped to title/sku/base_price_minor/description, price ₹→minor, unmapped cols → attributes, title-less row **flagged**); **column-map remembered per source signature** (header hash) in `tenant_settings`; advances `extracting→extracted` (or `failed`, resumable). `POST /v1/imports/{id}/extract` (`catalog:write`). Dep: **openpyxl** (MIT, approved). **+4 pytest** (CSV map+flag; XLSX; saved-mapping precedence; failure→failed). Photo/vision extract (MVP-077) stays gated. |
| Bill-B2 | P4.6 Financial dashboard (web-ops) | ✅ | 2026-08-08 | `01e2646` | **frontend-only** (`web-ops`, consumes B1). `FinancialSection` at `/financial`: rollup cards (**MRR / service revenue / cost / margin / active clients**) + **plans** manager (list/create) + **per-client billing** (store picker → assign plan + record charge [amount + cost] + view charges). Dashboard on `platform.tenants:read`; write actions gated `tenants:manage` (dev/admin) client-side + server-side. Honest note: cashflow/burn/runway need expense+cash inputs (deferred). **vitest 6** (nav pins updated); lint/tsc/build/guards green. Completes **P4.6 Financial** (Sales = separate GO-pipeline ticket). |
| Bill-B1 | Per-client billing model + operator CRUD | ✅ | 2026-08-08 | `4e06f82` | Unblocks P4.6 Financial. Migration 035: `billing_plans` (global GO tiers), `billing_subscriptions` (org-scoped RLS, one active/client), `billing_charges` (org-scoped RLS: **amount_minor + cost_minor** → margin), + `platform_billing_rollup()` **SECDEF** (MRR = Σ active plan prices, this-month revenue/cost/**margin**, active clients). **Operator-only** `/v1/admin/billing/*` (plans, per-client subscription + charges, rollup) — admin-plane + `platform.tenants:manage/read`, **scoped writes audited w/ target_org**; no tenant path; flag allowlist untouched. Model: managed-budget+margin, named tiers (founder). **+5 pytest** (margin=amount−cost + MRR via rollup deltas; charges org-isolated; 403/401/404). |
| Camp-C2 | Campaign compose/send UI (MVP-089) | ✅ | 2026-08-08 | `1356b53` | **frontend-only** (`web/`) + a tiny `GET /v1/campaigns/audience-preview`. `CampaignsSection` at `/campaigns`: list (status/sent/failed/halt-reason), create (name + **approved-template** picker → draft), and the **send wizard** — audience preview count → **type the number to confirm** (the C5 typed-count gate; 409 shows the real number) → parks a tier-3 approval that appears in the existing Approvals queue. Nav gated `campaigns:read` (owner/manager/viewer; **staff excluded** — pinned nav test restructured). Gated-simulated. **vitest 50**; lint/tsc/build/guards green. |
| Camp-C1 | Campaign SEND execute path (MVP-075 / C5) | ✅ | 2026-08-08 | `291dd68` | **Closes the top MVP gap** (campaigns could be created + analyzed but never SENT). Full faithful spec: migration 034 (`campaign_sends` +RLS + template/halt cols + widened status). `POST /v1/campaigns/{id}/send` **typed-count gate** (409 on mismatch, no silent fix) → **tier-3 approval**. On approve (consumer on `approval.resolved`): **staggered fan-out ≤500/hr** → per recipient get-or-create conversation + mint audit-cap + execution token + gated `send()` template (marketing) — **its per-send consent/suppression gates ARE the re-check** — write `campaign_sends` row → emit `campaign.executed.v1` → mark executed. **Quality-halt** on opt-out spike (or red Meta rating). `campaign_fanout` hourly scheduler resumes stagger. Audience = consented + un-suppressed (matches send()'s policy). Gated-simulated. **+5 pytest** (full fan-out sends 2/2 & executes; typed-count 409; tier-3; reject sends nothing; opt-out halt). |
| P4.5 | Operator console: per-store drill-down | ✅ | 2026-08-08 | `cde833e` | Phase 4 store insight drill-down (the most sensitive surface: a store's actual insight CONTENT). Migration 033 two **SECDEF** fns `platform_store_reports(org)` (summaries) + `platform_store_report(org, report)` (full, **scoped to org_id=p_org** so a report can't be fetched under the wrong store). `GET /v1/admin/tenants/{org}/reports(/{id})` gated on **`platform.insights:read`** + admin-plane, **each read audited with `target_org_id`**. web-ops: roster store names link → `/stores/$orgId` `StoreReportsSection` (verdict list → expand drivers/numbers). Flag allowlist untouched. **+6 pytest** (list scoping; detail; **cross-store id 404**; 403/401/404). |
| P4.4 | Operator console: customer success | ✅ | 2026-08-08 | `c1e202d` | Phase 4 store-health. Migration 032 `platform_customer_health()` **SECDEF** fn → one row **per store**: paused, open/urgent/resolved_7d tickets, days_since_activity, revenue_7d/prev, computed **`at_risk`** (paused OR urgent OR inactive>14d OR revenue halved WoW) — **no PII**, flag allowlist untouched. `GET /v1/admin/customer-health` (`platform.tenants:read` + admin-plane, **audited**); web-ops `CustomerSuccessSection` at `/health` (at-risk-first table w/ reason chips + trend header). **NPS/upsell deferred** (need surveys/billing — not faked). **+4 pytest** (at_risk fires per cause + healthy stays clear; 403/401/404); web-ops vitest 6. |
| P4.3 | Operator console: executive + marketing | ✅ | 2026-08-08 | `97663fc` | Phase 4 cross-store rollup. Migration 031 `platform_analytics_rollup(days)` **SECDEF** fn → one row of platform-wide sums/counts over a window **+ prior window (WoW)**: revenue/orders/leads/quotes (+prev), active_stores, campaigns_run, messages_sent, campaigns_analyzed, attributed_revenue — **no PII**, flag allowlist untouched. `GET /v1/admin/analytics/rollup?days=` (`platform.tenants:read` + admin-plane, **audited**); web-ops `AnalyticsSection` at `/analytics` (Executive WoW cards + Marketing cards). **CAC/churn + impressions/CPL deferred** (need billing/ad data — not faked). **+4 pytest** (before/after deltas incl. prior-window; 403/401/404); web-ops vitest 6. |
| P4.2 | Operator console: operational dashboard | ✅ | 2026-08-08 | `de937de` | Phase 4 "what's breaking/delayed". Migration 030 `platform_operational_health()` **SECDEF** fn → one row of curated COUNTS (outbox_pending/stuck, approvals_pending/overdue, tickets_open/urgent, stores_paused) — **no PII**, flag allowlist untouched. `GET /v1/admin/ops/health` (`platform.tenants:read` + admin-plane, **audited**); web-ops `OperationalSection` at `/ops` (severity-colored health cards + GlitchTip-for-detail note). **+4 pytest** (before/after deltas prove counts move exactly + overdue≠pending discrimination; 403/401/404); web-ops vitest 6 (nav pins updated). |
| P4.1 | Operator console: cross-store roster | ✅ | 2026-08-08 | `98ecb05` | Phase 4 foundation. Migration 029 `platform_tenant_roster()` **SECURITY DEFINER** fn → curated per-store rows (id, name, plan, status, created_at, paused, open_tickets, member_count) — **no customer PII**; reads RLS-protected settings/tickets/user_orgs via definer **without widening the `app.platform_admin` flag** (least-privilege lock stays green). `GET /v1/admin/tenants` (`platform.tenants:read` + admin-plane, **audited**); web-ops `StoresSection` roster table (replaces placeholder). **+5 pytest** (reflects paused/counts; curated-fields-only/no-PII; 403/401/404 gates); web-ops lint/tsc/vitest 6/build. Verified vs pg16. |
| Sec-S3 | Security hardening: backup + tested restore | ✅ | 2026-08-08 | `2b4182f` | audit #16e — the **restore drill** is the point (an untested backup is false safety). `scripts/db_backup.sh` / `db_restore.sh` (guardrails: refuses `*prod*` + primary-DB without `--force`) / `db_restore_drill.sh` (dump→restore-into-scratch→verify table count + alembic head + org rows→drop). **Drill runs in CI** (`migrate` job) every push = continuous proof; `make backup-drill` locally (in-container, no host tools). `infra/db/BACKUP_RESTORE.md`; `/backups/` gitignored. **Verified pg16: 71 tables, head 9f9334d2999a, orgs round-tripped → PASS.** |
| Sec-S2 | Security hardening: error tracking (GlitchTip) | ✅ | 2026-08-08 | `02038dd` | audit #16d — **self-hosted GlitchTip** (Sentry-compatible; data stays on our cloud). Backend `core/common/error_tracking.py` + `sentry-sdk` (gated on `error_tracking_dsn`, **off by default**; `send_default_pii=false`, bodies/locals dropped, `before_send` scrubs phone/OTP/email/tokens); frontend `@sentry/react` + `ErrorBoundary` + `lib/errorTracking` (gated on `VITE_ERROR_DSN`, same scrubbing); `docker-compose.glitchtip.yml` + `GLITCHTIP.md` + `make glitchtip`. **+5 pytest, +3 vitest.** off-by-default proven; scrubbers unit-tested. |
| Sec-S1 | Security hardening: secret scanning | ✅ | 2026-08-08 | `bc6e4a6` | audit #16a — `.gitleaks.toml` (default ruleset + **tight** allowlist: one confirmed false positive `private_key: Ed25519PrivateKey` param annotation + SOPS `*.enc.yaml` by-design) + CI `secret-scan` job (pinned gitleaks 8.30.1, **full history**, `--redact`) + `make secret-scan`. **Recon verdict: 134 commits, zero real secrets.** Verified the scanner still bites a fake token. |
| Phase-3.6 | Settings + autonomy volume-knob | ✅ | 2026-08-07 | `48f4117` | autonomy **wired live** (Option A) — `engine.evaluate_tool` max-tier overlay (`auto` respects pack tiers, else/paused → approval); **tier-4 money floor immovable at every knob position**; free-dial (retires tighten-only), `autonomy.campaigns`+`autonomy.paused`; `GET /v1/settings/autonomy`; `SettingsSection` (pause + Auto/Review + locked floor + prefs); settings-change audit already existed. **7 pytest + vitest 37**. Completes Phase 3 |

> **Gap note (resolved 2026-07-22):** MVP-006–010 were leapfrogged after the scaffold; the
> founder directed implementing them before continuing. Now done/scaffolded on branch
> `feature/mvp-006-010-platform-foundations`: **007 + 010 fully; 006 + 008 partial** (downstream
> deps / founder key); **009 BLOCKED** (needs Hetzner account, domain, residency decision, Meta).

---

## What each completed ticket actually delivered

### MVP-001–005 — Monorepo scaffold (2026-07-10, `cf7536e`)
Bundled in one commit per the scaffold prompt:
- **001** — repo layout: full `core/` module tree (17 submodules as stubs), `web/` (Vite + React + TS + Tailwind), `migrations/`, `infra/docker/`, `Makefile`, `pyproject.toml`.
- **002** — `infra/docker/docker-compose.dev.yml`: Postgres (pgvector) + Redis + api/worker/scheduler/caddy.
- **003** — `.github/workflows/ci.yml`.
- **004** — Alembic async migration framework + `migrations/lib/rls.py` (`apply_rls`/`drop_rls`).
- **005** — `core/common/config.py` (layered settings) + `core/common/errors.py` (RFC7807 + the 12 canonical error codes).

At the time, 002–005 were "present but not fully verified." Since then, MVP-011 verified 002 (data services) and 004 (migration up/down) for real against a live database.

### MVP-011 — OTP auth endpoints (2026-07-22, `6cd38f4`)
- **Migration 001** — global `users`, `sessions`, `otp_challenges` (no RLS; membership deferred to `user_orgs`/002). Verified live: upgrade + downgrade round-trip.
- **Auth logic** (`core/tenancy/auth.py`) — E.164 + email validation, argon2 hashing, OTP challenge state machine (5m expiry / ≤5 attempts / 60s resend), JWT mint/decode (15m access / 30d refresh).
- **API** — `POST /v1/auth/otp`, `POST /v1/auth/otp/verify`; failures use plain HTTP (401/422/429), never canonical error codes.
- **Interim email channel** — `GROWTH_OPERATOR_OTP_CHANNEL` (default `email`) because Meta WhatsApp is **pending API access, not deferred**; phone path retained behind the flag.
- **`EmailOtpDelivery`** — SMTP adapter, **gated OFF by default** (real send needs provider + creds + enable).
- **Tests** — 45 unit + 3 live-DB integration (request→verify→tokens, wrong-code, lockout).
- Also on this branch (not separate tickets): docker-compose env-fix (BLOCKERS #1), a login-page demo (MVP-082 preview slice), and the live-DB integration harness.

---

## Process (per founder request, 2026-07-22)
Every completed ticket gets **a dated row here + a dated entry in IMPLEMENTATION_LOG.md**, using the actual calendar date of implementation. Update this file the day work lands.
