"""conversations — read-only inbox/thread/lead-pipeline views for the owner dashboard (Ticket 3.3).

No writes: replying happens through the mediated, approval-gated send path (MVP-054), never here.
"""
