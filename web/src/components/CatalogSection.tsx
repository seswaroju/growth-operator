import ComingSoon from "./ComingSoon";

// Ticket 3.4 fills this with the catalog table + item detail + search + rate-linked pricing.
export default function CatalogSection() {
  return (
    <ComingSoon
      title="Catalog"
      description="Your products, prices, and live gold-rate-linked quotes will be managed here."
    />
  );
}
