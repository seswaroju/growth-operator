import ComingSoon from "./ComingSoon";

// Ticket 3.5 fills this with the CRM (customer list + profile: their conversations, leads, quotes).
export default function CustomersSection() {
  return (
    <ComingSoon
      title="Customers"
      description="Everyone who's reached out — with their history, preferences, and past purchases — will live here."
    />
  );
}
