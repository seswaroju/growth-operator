import ComingSoon from "./ComingSoon";

// Ticket 3.6 fills this with the store profile + the autonomy "volume knob" (owner-only), within the
// fixed money floor (DECISIONS 2026-08-05).
export default function SettingsSection() {
  return (
    <ComingSoon
      title="Settings"
      description="Your store profile and how much your assistant handles on its own — the autonomy dial — will be set here."
    />
  );
}
