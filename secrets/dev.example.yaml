# EXAMPLE ONLY — fake values, safe to commit. Copy to a plaintext file, replace with real
# values, and `sops --encrypt` it to secrets/dev.enc.yaml (never commit the plaintext).
# Keys here map to GROWTH_OPERATOR_* settings in core/common/config.py (without the prefix).
jwt_secret: "replace-me-with-a-long-random-string"
smtp_host: "smtp.example.com"
smtp_username: "apikey"
smtp_password: "SG.fake-not-a-real-key"
smtp_from: "no-reply@example.com"
